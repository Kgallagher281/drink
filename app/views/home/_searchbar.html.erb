<div class="search">

	<%= form_tag search_path, :method => 'get' do %>
		<p>
		<%= text_field_tag :search, params[:search], :class => 'search' %>
		<%= submit_tag "Ingredient Search", :name => nil%>
		</p>
	<% end %> 

</div>